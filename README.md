# Wales Weather-to-Rail Impact Predictor

A machine learning + visualization project that predicts train delay minutes in Wales from historical weather
(Met Office) by aligning events by **nearest timestamp** and **nearest weather station**.

## 1) Setup
```bash
python -m venv .venv
# Windows:
.venv\Scripts\activate
pip install -r requirements.txt
